apiVersion: v1
kind: Service
metadata:
 name: frontend-service
 labels:
   app: skill-hire
   tier: frontend

spec:
 ports:
   - port: 8080
     targetPort: 8080
 selector:
  app: skill-hire
  tier: frontend
 type: ClusterIP