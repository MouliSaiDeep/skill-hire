apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: frontend-route
  labels:
   app: skill-hire

spec:
 to:
  kind: Service
  name: frontend-service
 port:
  targetPort: 8080
 tls:
  termination: edge
  insecureEdgeTerminationPolicy: Redirect